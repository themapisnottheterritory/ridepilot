- if @past_location
  infoPopup.type = "PastLocation";
  infoPopup.itin_id = #{@past_location.id};
  infoPopup.setContent("#{ j render 'vehicle_info_window', location: @past_location, is_real_time: false }");
