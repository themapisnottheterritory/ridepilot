!.keep
*.DS_Store
*.swp
*~
/.bundle
/.sass-cache
/.vagrant
/.byebug_history
/cookbooks
/config/app_config.yml
/config/database.yml
/config/prod_deploy.rb
/config/uat_deploy.rb
/config/database.yml.local
/config/unicorn.production.rb
/db/*.sqlite3
/db/*.sqlite3-journal
/db/legacy
/log/*.log
/public/system
/public/cad_avl_data_archives
/tmp
# Ignore application configuration
/config/application.yml
/railsbox
/.byebug_history
/dump.rdbget-gcrpc-mail-users.py
get-mail-users.py
*.json

# --- secrets / keys: NEVER commit to this public repo ---
# Rails master key (decrypts credentials.yml.enc)
/config/master.key
# Encrypted credentials — env-specific and paired to a local master.key; prod secrets
# are provisioned in the environment, not committed to this public repo
/config/credentials.yml.enc
# Azure app-only script with a hardcoded (compromised) client secret
get-gcrpc-mail-users.py
# WireGuard tablet keys (private keys) — belongs in a vault
driver_tablet_keys.csv
# --- local runtime / scratch artifacts ---
destinations_FINAL.csv
/osrm-data/
/storage/
