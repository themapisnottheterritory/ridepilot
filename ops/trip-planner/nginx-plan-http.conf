# Goes in /etc/nginx/conf.d/plan-shared.conf (http context): rate limit + tile cache for plan.gcrpc.org
limit_req_zone $binary_remote_addr zone=geocode:10m rate=5r/s;
proxy_cache_path /var/cache/nginx/plan-tiles levels=1:2 keys_zone=tiles:50m max_size=2g inactive=30d;
